export interface DiscountButtonProps {
    px: number | string;
    py: number | string;
    price?: string;
    onClick?: () => void;
}

export interface CustomCheckboxProps {
    fontSize?: number;
}